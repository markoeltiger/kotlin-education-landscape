import { t as getServerFnById } from "./__23tanstack-start-server-fn-resolver-CxbQ988a.js";
import { d as TSS_SERVER_FUNCTION, t as createServerFn } from "./createServerFn-CIHAFgYl.js";
import { createFileRoute, lazyRouteComponent } from "@tanstack/react-router";
import { fallback, zodValidator } from "@tanstack/zod-adapter";
import { z } from "zod";
//#region node_modules/@tanstack/start-server-core/dist/esm/createSsrRpc.js
var createSsrRpc = (functionId) => {
	const url = "/_serverFn/" + functionId;
	const serverFnMeta = { id: functionId };
	const fn = async (...args) => {
		return (await getServerFnById(functionId, { origin: "server" }))(...args);
	};
	return Object.assign(fn, {
		url,
		serverFnMeta,
		[TSS_SERVER_FUNCTION]: true
	});
};
//#endregion
//#region src/routes/index.tsx
var $$splitComponentImporter = () => import("./routes-CheMnj0f.js");
var fetchDataset = createServerFn({ method: "GET" }).handler(createSsrRpc("20a17d7d8365cb927dea2118a3f6213a85e659927a15fefccfd0091075b01f45"));
var searchSchema = z.object({
	primary_only: fallback(z.boolean(), false).default(false),
	sources: fallback(z.array(z.string()), []).default([]),
	tiers: fallback(z.array(z.string()), []).default([]),
	learning_types: fallback(z.array(z.string()), []).default([]),
	countries: fallback(z.array(z.string()), []).default([]),
	min_stars: fallback(z.number(), 0).default(0),
	conf_min: fallback(z.number(), 0).default(0),
	conf_max: fallback(z.number(), 1).default(1),
	search: fallback(z.string(), "").default("")
});
var Route = createFileRoute("/")({
	validateSearch: zodValidator(searchSchema),
	loader: () => fetchDataset(),
	component: lazyRouteComponent($$splitComponentImporter, "component")
});
//#endregion
export { Route as t };
