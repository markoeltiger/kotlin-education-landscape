//#region src/lib/dataset.ts
async function fetchDataset() {
	try {
		const { getApiData } = await import("./api-data-D1F5Ldm6.js");
		const data = await getApiData();
		return {
			courses: Array.isArray(data.courses) ? data.courses : [],
			serp: data.serp || [],
			baseline: data.baseline || null,
			insights: data.insights || null,
			meta: data.meta || { generated_at: (/* @__PURE__ */ new Date()).toISOString() }
		};
	} catch (error) {
		console.error("Error fetching dataset from API, falling back to static files:", error);
		const [coursesResponse, serpResponse, baselineResponse, insightsResponse] = await Promise.all([
			fetch("/data/courses_unified.json"),
			fetch("/data/serp_progress.json"),
			fetch("/data/baseline_comparison.json"),
			fetch("/data/insights.json")
		]);
		let courses = [];
		let meta = { generated_at: (/* @__PURE__ */ new Date()).toISOString() };
		if (coursesResponse.ok) {
			const coursesData = await coursesResponse.json();
			if (Array.isArray(coursesData)) courses = coursesData;
			else if (coursesData.courses) {
				courses = coursesData.courses;
				meta = coursesData.meta || meta;
			}
		}
		const serp = serpResponse.ok ? await serpResponse.json() : [];
		const baseline = baselineResponse.ok ? await baselineResponse.json() : null;
		const insights = insightsResponse.ok ? await insightsResponse.json() : null;
		return {
			courses,
			serp,
			baseline,
			insights,
			meta
		};
	}
}
var emptyFilters = {
	sources: [],
	tiers: [],
	learning_types: [],
	countries: [],
	conf_min: 0,
	conf_max: 1,
	search: ""
};
function applyFilters(courses, f) {
	const q = f.search.trim().toLowerCase();
	const csSet = new Set(f.countries);
	const srcSet = new Set(f.sources);
	const tSet = new Set(f.tiers);
	const lSet = new Set(f.learning_types);
	return courses.filter((r) => {
		if (srcSet.size && !srcSet.has(r.source)) return false;
		if (tSet.size && !tSet.has(r.signal_tier)) return false;
		if (lSet.size && !lSet.has(r.learning_type)) return false;
		if (csSet.size && !csSet.has(r.country)) return false;
		if (r.kotlin_confidence < f.conf_min || r.kotlin_confidence > f.conf_max) return false;
		if (q) {
			if (!`${r.title} ${r.provider} ${r.country}`.toLowerCase().includes(q)) return false;
		}
		return true;
	});
}
function groupBy(rows, key) {
	const m = /* @__PURE__ */ new Map();
	for (const r of rows) {
		const k = key(r);
		m.set(k, (m.get(k) ?? 0) + 1);
	}
	return m;
}
function topN(m, n, dropEmpty = true) {
	const arr = Array.from(m.entries()).filter(([k]) => {
		if (!dropEmpty) return true;
		const keyStr = String(k);
		return keyStr && keyStr.trim().length > 0;
	});
	arr.sort((a, b) => b[1] - a[1]);
	return arr.slice(0, n);
}
//#endregion
export { applyFilters, emptyFilters, fetchDataset, groupBy, topN };
